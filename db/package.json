{
  "name": "agentv-db",
  "private": true,
  "version": "0.1.0",
  "description": "AgentV control-plane schema + migrations",
  "scripts": {
    "migrate": "knex migrate:latest --knexfile knexfile.js",
    "rollback": "knex migrate:rollback --knexfile knexfile.js",
    "status": "knex migrate:status --knexfile knexfile.js"
  },
  "dependencies": {
    "knex": "^3.1.0",
    "mysql2": "^3.11.0",
    "dotenv": "^16.4.5"
  }
}
