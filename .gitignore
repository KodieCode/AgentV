# Secrets — never commit
.env
**/.env
**/.api-token

# Runtime / per-deployment state
**/notifications/inbox.jsonl
**/notifications/archive/
**/memory/*-snapshot*
**/logs/
*.log
**/journal/*.diff

# Node
node_modules/
dist/
.DS_Store

# Provision-time / runtime (created on deploy, never committed)
**/.claude/skills/shared
**/dist/
control-plane/*/dist/

# memory-search FTS5 index — local cache per agent, never committed
**/data/memory.sqlite

# in-place updater state (applied upgrade-steps ledger + version marker)
.agentv-version
